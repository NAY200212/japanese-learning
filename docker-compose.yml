services:
  mysql:
    image: mysql:8.0
    container_name: jp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
      MYSQL_DATABASE: japanese_learning
    ports:
      - "3308:3306"          # ホスト 3308 → コンテナ 3306。ローカル 3307 との衝突を回避
    volumes:
      - mysql-data:/var/lib/mysql
      - ./docker/initdb:/docker-entrypoint-initdb.d   # 初回起動時に建表とデータ投入を自動実行
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p123456"]
      interval: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: jp-redis
    ports:
      - "6380:6379"

  backend:
    build: .
    container_name: jp-backend
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: root
      MYSQL_PASSWORD: "123456"
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started

volumes:
  mysql-data:
