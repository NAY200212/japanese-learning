server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/japanese_learning?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true

knife4j:
  enable: true

# JWT 配置：secret 是签名密钥（随便写个长字符串），expiration 是过期时间（毫秒）
jwt:
  secret: "japanese-learning-secret-key-2026-keep-it-long"
  expiration: 8640000000   # 2400 小时

